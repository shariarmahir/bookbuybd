import Dashboard from '@/components/Dashboard/dashboard';

export const metadata = {
    title: 'Dashboard - BookBuyBD',
    description: 'Manage your bookstore inventory, orders, and more.',
};

export default function DashboardPage() {
    return <Dashboard />;
}
